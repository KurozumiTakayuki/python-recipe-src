# 絶対値の計算
val = -100
abs_val = abs(val)
print("絶対値", abs_val)

# 合計、最大、最小の計算
val_list = [100, 76, 5, -9, 25, 3.5, -0.99]
sum_val = sum(val_list)
max_val = max(val_list)
min_val = min(val_list)

print("合計", sum_val)
print("最大", max_val)
print("最小", min_val)
